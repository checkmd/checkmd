// Generated by CoffeeScript 1.6.2
(function() {
  define(['jquery', 'backbone_tastypie', 'collections/steps'], function($, Backbone, StepCollection) {
    var ChecklistModel;

    return ChecklistModel = Backbone.Model.extend({
      url: function() {
        var url;

        url = "/api/v1/checklist/";
        if (this.get('_id')) {
          url += "" + (this.get('_id')) + "/";
        }
        return url;
      },
      parse: function(response) {
        var steps;

        steps = new StepCollection();
        steps.add(response.steps);
        return this.set("steps", steps);
      },
      forTemplate: function() {
        var json;

        json = this.toJSON();
        json.steps = this.get('steps').toJSON();
        return json;
      }
    });
  });

}).call(this);
